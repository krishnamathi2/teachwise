# Frontend environment variables (copy to frontend/.env.local and fill values)

# Supabase Auth
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Stripe Checkout
# Optional: front-end can pass a default priceId to the proxy
NEXT_PUBLIC_STRIPE_PRICE_ID=
